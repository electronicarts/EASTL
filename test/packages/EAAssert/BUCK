prebuilt_cxx_library(
  name = 'eaassert',
  header_namespace = 'EAAssert',
  header_only = True,
  exported_headers = subdir_glob([
    ('include/EAAssert', '**/*.h'),
  ]),
  visibility = [
    'PUBLIC',
  ],
)
